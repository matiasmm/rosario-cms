{% extends '::base.html.twig' %}

{% block page_title %}<h1>Contenidos</h1>{% endblock %}

{% block body %}
    {% for content in contents %}
        <div>
            <h3>
                <a href="{{ path('cms_frontend_content_show', {id: content.id}) }}">{{ content.title }}</a>
            </h3>
        </div>
    {% endfor %}
{% endblock %}